/**
 * Shared Components Index
 * Re-export all shared components for easier importing
 */

export { default as ErrorBoundary } from './ErrorBoundary';
export { default as LoadingSpinner } from './LoadingSpinner';

